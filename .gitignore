# ===========================================
# Generic Build Artifacts (C++)
# ===========================================
# C++ objects and libs
*.slo
*.lo
*.o
*.a
*.la
*.lai
*.so
*.so.*
*.dll
*.dylib
*.lib
*.obj
*.exe
*.out
*.pdb
*.ilk

# CMake
CMakeCache.txt
CMakeFiles/
CMakeScripts/
cmake_install.cmake
install_manifest.txt
compile_commands.json # Generated by CMake or QtCreator
# Build directories (added)
build/
_build/
cmake-build-*/
# CMake artifacts
Makefile
Makefile.*
conanbuildinfo*.txt
conaninfo.txt
*.cmake
# Exception for triplet files
!/triplets/*.cmake
!/cmake/*.cmake
# CMake temporary files
CMakeDoxygenDefaults.cpp
Testing/
# CMake config files
CMakeLists.txt.user*

# Ninja
.ninja_log
.ninja_deps
build.ninja

# ===========================================
# Qt (and QML)
# ===========================================
object_script.*.Release
object_script.*.Debug
*_plugin_import.cpp
/.qmake.cache
/.qmake.stash
*.pro.user
*.pro.user.*
*.qbs.user
*.qbs.user.*
*.moc
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h
*.qmlc
*.jsc
Makefile*
*build-*
build/* # Important: prevent builds inside a build/ subfolder
*.qm
*.prl

# Qt unit tests
target_wrapper.*

# QtCreator
*.autosave

# QtCreator Qml
*.qmlproject.user
*.qmlproject.user.*

# QtCreator CMake
CMakeLists.txt.user*
.qtcreator/*
# QtCreator 4.8< compilation database
compile_commands.json

# QtCreator local machine specific files for imported projects
*creator.user*

# ===========================================
# Operating System
# ===========================================
# Windows temp files
Thumbs.db
ehthumbs.db
Icon?
~$*
*.tmp
*.bak
*.swp
*~
.DS_Store # macOS
.Spotlight-V100 # macOS
.Trashes # macOS
._* # macOS
.vagrant/
*.fuse_hidden*

# ===========================================
# Package Managers
# ===========================================
/vcpkg_installed/*

# ===========================================
# Development Tools
# ===========================================
# Vim
*.swp
*.swo
*~

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# VSCode
.vscode/
*.code-workspace

# CLion
.idea/
*.iws
*.iml
*.ipr

# Visual Studio
*.VC.db
*.VC.VC.opendb
*.user
*.userosscache
*.sln.docstates
*.vcxproj.filters
[ip]/*.user
[ip]/*.suo
[ip]/*.cachefile
[ip]/*.VC.VC.opendb
[ip]/*.VC.db
[ip]/*.userosscache
[ip]/*.sln.docstates
[ip]/*.vcxproj.filters
.vs/*

# ===========================================
# Capture Moment Project Specifics
# ===========================================

# Build directories (repetition for clarity, but build/ is already at the top)
build-*/
_build-*/
cmake-build-*/

# Output executables
capturemoment_desktop
capturemoment_mobile

# OIIO cache files (should not be versioned)
# These files can be generated dynamically
# and are specific to the user's machine or runtime
OIIO_cache/
cache/
.imagecache/

# Thumbnail cache files generated by the app
.cache/thumbnails/
thumbnails_cache/

# Catalog database (if generated at runtime). TO ADD
# Ex: If the program generates a DB on open
#*.db
# capturemoment_catalog.db


# XMP sidecar files generated by the program. TO ADD
# These are created for *each image*.
# Git ignores global "*.xmp", but NOT XMP files associated with specific images
# (e.g., if we have "example.CR2", we don't want to ignore "example.CR2.xmp"
#*.xmp

# Log directories
logs/
*.log

# User config files (saved by the program)
# If your program saves preferences to a local file
# e.g., settings.ini, config.json
settings.ini
config.json
capturemoment_settings.json

# App temporary files
tmp/
temp/

# Halide (added)
# Halide might generate temporary files or JIT objects
*.halide_func
*.jit
# JIT cache folder (if used)
.halide_cache/

# OpenColorIO (added)
# If you have local or temp generated OCIO config files
# or temporary LUTs
*.ocio.config.tmp
# LUT cache folder (if your app creates one)
.cache/luts/

# Packages generated by CPack or other tools
# (e.g., .deb, .rpm, .msi, .dmg, .appimage)
*.deb
*.rpm
*.msi
*.dmg
*.AppImage
# Packaging staging folders
package/
_packages/

# Generated documentation (Doxygen, Sphinx, etc.)
docs/_build/
docs/api/
apidoc/

# Test temp files or benchmark results
tests/output/
tests/benchmark_results.json
benchmark_results_*.json
# If you have dynamically generated test files
tests/generated_*.tiff
tests/processed_*.jpg

# ===========================================
# End of .gitignore
# ===========================================
